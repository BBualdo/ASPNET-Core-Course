@model CountryList

<div class="box">
  <h3>@Model.ListName</h3>
  <table class="table w-100">
    <thead>
      <th>Country</th>
      <th>Capital City</th>
    </thead>
    @foreach (Country country in Model.ListItems)
    {
      <tr>
        <td>@country.Name</td>
        <td>@country.CapitalCity</td>
      </tr>
    }
  </table>
</div>